info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "faisal@gmail.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
